# Agent2 终端交互界面 (TUI) 产品需求文档 (PRD)

## 1. 文档概述

### 1.1 背景与目标
随着大语言模型（LLM）在软件开发领域的深入应用，开发者对终端原生（Terminal-native）智能体（Agent）的需求日益增长。Agent2 TUI 旨在为开发者提供一个高效、沉浸、可控的终端命令行交互界面，将自动化代码理解、多步骤任务规划、文件读写、命令执行与工具调用（MCP）无缝融合到日常命令行工作流中。

### 1.2 目标用户
- 习惯在终端环境（Linux / macOS / WSL）中进行开发、排错与运维的软件工程师。
- 需要借助 AI 辅助阅读大型代码库、执行自动化重构或编写单元测试的开发者。

---

## 2. 核心功能需求 (Feature Specifications)

### 2.1 模型与推理参数管理 (Model Management)
| 需求编号 | 功能点 | 详细描述 | 优先级 |
| :--- | :--- | :--- | :--- |
| **FR-MOD-01** | 动态模型切换 | 支持在活跃会话中通过快捷指令（如 `/model`）呼出交互式选择菜单，实时切换底层模型（如 Claude 3.7/3.5 Sonnet, GPT-4o, o3-mini 等），无需重启客户端。 | P0 |
| **FR-MOD-02** | 思考深度配置 | 针对支持 Extended Thinking / CoT 的推理模型，提供 `/thinking` 开关及思考深度档位调节（如 Off / Low / Medium / High）。 | P1 |
| **FR-MOD-03** | Token 预算与消耗提示 | 在状态栏或命令提示符旁实时显示当前上下文占用的 Token 数、累计消耗量及预估费用。 | P2 |

### 2.2 会话与上下文生命周期 (Session & Context)
| 需求编号 | 功能点 | 详细描述 | 优先级 |
| :--- | :--- | :--- | :--- |
| **FR-SES-01** | 会话持久化与恢复 | 自动保存本地历史会话。通过 `/resume` 或会话列表面板选择历史会话进行无缝恢复，支持搜索历史会话标题与关键词。 | P0 |
| **FR-SES-02** | 上下文清除与重置 | 支持 `/clear`（清空终端视窗显示）与 `/new`（彻底重置会话上下文，开启全新任务）。 | P0 |
| **FR-SES-03** | 显式文件/目录注入 | 支持通过 `#file <path>`、`#dir <path>` 或 `@workspace` 语法快速注入指定代码文件、目录结构或符号信息。 | P0 |
| **FR-SES-04** | 上下文压缩与修剪 | 提供 `/compact` 指令，触发 Agent 自动对历史多轮对话及工具输出进行语义摘要，释放上下文窗口容量。 | P1 |

### 2.3 工具调用与安全确认机制 (Tool Execution & HITL)
| 需求编号 | 功能点 | 详细描述 | 优先级 |
| :--- | :--- | :--- | :--- |
| **FR-EXE-01** | 结构化工具状态展示 | 当 Agent 调用 `read_file`、`edit_file`、`grep`、`bash` 或 MCP 工具时，以带边框的状态卡片（Status Card）展示调用参数、执行中状态（Spinner）及执行结果摘要。 | P0 |
| **FR-EXE-02** | 人在回路交互确认 | 执行具有副作用的操作（如文件写入、Shell 命令执行、Git commit/push）前，必须阻塞并提示用户审批：`[y] 同意 / [n] 拒绝 / [a] 始终允许 / [e] 编辑参数`。 | P0 |
| **FR-EXE-03** | 执行权限策略 | 支持配置自动审批白名单（如只读文件查询、静态分析工具自动放行，破坏性命令必须确认）。 | P1 |
| **FR-EXE-04** | 紧急中断与接管 | 用户在 Agent 思考、输出或工具执行期间按下 `Ctrl+C` 或 `Esc`，立即终止后续流水线并保留已生成内容，控制权返还用户。 | P0 |

### 2.4 思考过程与任务计划看板 (Thinking & Task Planner)
| 需求编号 | 功能点 | 详细描述 | 优先级 |
| :--- | :--- | :--- | :--- |
| **FR-THK-01** | CoT 折叠与展开 | 推理模型的思考过程默认处于折叠状态（显示如 `Thinking (12s)...`），支持按快捷键（如 `Tab` 或回车）展开查看完整逻辑推导。 | P0 |
| **FR-THK-02** | 动态任务计划表 (Planner) | 面对复杂复合任务时，Agent 生成清晰的 Checklist 待办列表，实时渲染每一步的状态（`[ ] 待办`, `[-] 进行中`, `[✓] 完成`, `[x] 失败`）。 | P1 |

### 2.5 代码修改 Diff 预览与版本控制 (Diff & Rollback)
| 需求编号 | 功能点 | 详细描述 | 优先级 |
| :--- | :--- | :--- | :--- |
| **FR-DIF-01** | 终端内 Inline Diff | Agent 提议修改代码时，在终端内以红绿语法高亮渲染标准 Unified Diff 视窗，清晰标注变更文件名及行号。 | P0 |
| **FR-DIF-02** | 单步撤销与回滚 | 提供 `/undo` 指令，支持一键撤销 Agent 最近一次文件修改操作，恢复至修改前状态。 | P1 |

### 2.6 交互体验与富文本支持 (Terminal UX)
| 需求编号 | 功能点 | 详细描述 | 优先级 |
| :--- | :--- | :--- | :--- |
| **FR-UI-01** | 多行输入与快捷键 | 支持 `Shift+Enter` 或 `Alt+Enter` 换行；支持粘贴包含格式的代码片段时不触发误提交。 | P0 |
| **FR-UI-02** | Markdown 与语法高亮 | 终端输出支持完整的 Markdown 解析（表格、代码块语法高亮、引用、有序/无序列表等）。 | P0 |
| **FR-UI-03** | 斜杠命令补全 | 输入 `/` 时自动弹出命令补全提示框（Autocomplete Popup），支持上下箭头选择并附带简短说明。 | P1 |

---

## 3. 非功能性需求 (Non-Functional Requirements)

1. **终端兼容性**：
   - 完美适配 ANSI/xterm-256color 及 TrueColor 终端模拟器（iTerm2, Alacritty, Kitty, Windows Terminal 等）。
   - 支持动态窗口重绘（`SIGWINCH` 信号），自适应不同窗口宽高。
2. **性能与响应延迟**：
   - TUI 框架渲染主循环帧率保持在 30~60 FPS，输入无粘滞感与卡顿。
   - 工具调用及流式字符渲染不得阻塞主事件循环。
3. **扩展性**：
   - 原生支持 Model Context Protocol (MCP)，可通过本地 `mcp_config.json` 动态加载扩展工具集。

---

## 4. 界面布局与交互设计示意 (Wireframe / Layout)

```text
┌─ Agent2 CLI ─────────────────────────────────────────── [DeepSeek V4 Flash | Token: 12.4k]               ─┐
│                                                                                                           │
│  User > 请重构 src/parser.rs 中的 ast 遍历逻辑，改用 visitor 模式并添加对应单元测试。                               │
│                                                                                                           │
│  Agent:                                                                                                   │
│  ▶ Thinking Process (Expanded, 4.2s)                                                                      │
│    └─ 1. 分析当前 AST 结构体定义与循环处理逻辑...                                                            │
│    └─ 2. 构造 Visitor trait，抽象 Expr 与 Stmt 访问接口...                                                  │
│                                                                                                           │
│  [Task Plan]                                                                                              │
│    [✓] 1. 读取 src/parser.rs 及 src/ast.rs                                                                │
│    [-] 2. 实现 AST Visitor 接口并在 src/visitor.rs 中落地                                                 │
│    [ ] 3. 运行 cargo test 验证现有测试                                                                    │
│                                                                                                           │
│  ⚙ Tool Call: read_file ("src/parser.rs") -> Success (182 lines)                                          │
│                                                                                                           │
│  📝 Proposed Changes to `src/parser.rs`:                                                                  │
│  ───────────────────────────────────────────────────────────────────────────────────────────────────────  │
│  @@ -45,7 +45,6 @@ impl Parser {                                                                          │
│  -    pub fn traverse(&self, node: &Node) {                                                               │
│  +    pub fn accept<V: Visitor>(&self, visitor: &mut V) {                                                 │
│  ───────────────────────────────────────────────────────────────────────────────────────────────────────  │
│  Apply these changes? [y/n/always/edit]:                                                                  │
│                                                                                                           │
└───────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
